git 저장소 초기화

1. 로컬 디렉토리에 .git을 삭제

2. git add로 파일들을 추가
git commit -m 'Initial commit'

4.초기화 시킬 원격 저장소를 추가.

git remote add origin <url>

5. 현재 상태를 원격저장소에 적용.

 git push --force --set-upstream origin master
